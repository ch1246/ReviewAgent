# AI Agent 项目架构设计文档

## 目录

1. [整体架构](#1-整体架构)
2. [LangGraph 编排与状态设计](#2-langgraph-编排与状态设计)
3. [多 Agent 协作](#3-多-agent-协作)
4. [Function Calling / Tool 工具调用](#4-function-calling--tool-工具调用)
5. [上下文管理与 Token 成本](#5-上下文管理与-token-成本)
6. [记忆系统：短期记忆与长期记忆](#6-记忆系统短期记忆与长期记忆)
7. [RAG 系统：文本分块、Embedding、向量检索](#7-rag-系统文本分块embedding向量检索)
8. [Redis 在 Agent 中的角色](#8-redis-在-agent-中的角色)
9. [提示词设计](#9-提示词设计)
10. [可靠性与异常处理](#10-可靠性与异常处理)
11. [技术选型建议](#11-技术选型建议)

---

## 1. 整体架构

```
用户输入
  │
  ▼
┌─────────────┐
│  意图识别/路由  │
└──────┬──────┘
       │
  ┌────┼────────────┐
  ▼    ▼            ▼
直接  RAG检索     工具调用
回答    │            │
  │    ▼            ▼
  │  向量检索     执行工具
  │    │            │
  └────┼────────────┘
       ▼
   结果整合/合成
       │
       ▼
     输出
```

核心模块：

| 模块 | 职责 |
|------|------|
| LangGraph 编排 | 定义 Agent 工作流，管理状态流转 |
| 上下文管理 | 控制 token 用量，压缩历史对话 |
| 记忆系统 | 短期（会话内）+ 长期（跨会话持久化） |
| RAG 系统 | 文本分块 → embedding → 向量检索 → 召回 |
| 工具系统 | function calling 定义、执行、错误处理 |
| Redis | 缓存、会话存储、限流 |

---

## 2. LangGraph 编排与状态设计

LangGraph 用**有向图**定义 Agent 工作流。节点 = 处理步骤，边 = 条件路由，**State** 是贯穿全流程的共享数据结构。

### 2.1 完整 State 定义

AgentState 是一个全局共享的数据结构，每个节点都可以读写。它包含以下字段：

**对话与消息**

| 字段 | 类型 | 说明 |
|------|------|------|
| messages | 消息列表 | 当前对话的完整消息（用户输入、AI 回复、工具结果），自动追加不会覆盖历史 |

**意图与路由**

| 字段 | 类型 | 说明 |
|------|------|------|
| user_intent | 字符串 | 意图分类结果，如 "qa" / "tool_call" / "chitchat" / "rag_query"，由意图识别节点写入 |
| route_decision | 枚举 | 路由节点的决策：`direct_answer` / `rag_retrieval` / `tool_execution` / `handoff`，决定走哪条分支 |

**RAG 相关**

| 字段 | 类型 | 说明 |
|------|------|------|
| query | 字符串 | 原始用户问题，用于 RAG 检索 |
| rewritten_query | 字符串 | 查询改写后的检索词（如果原始问题不适合直接检索） |
| retrieved_docs | 文档列表 | 向量检索召回的文档列表，每个包含 content、source、score |
| retrieval_confidence | 浮点数 | 召回置信度 (0~1)，低于阈值时触发兜底策略 |

**工具调用相关**

| 字段 | 类型 | 说明 |
|------|------|------|
| tool_calls | 工具调用列表 | LLM 决定调用的工具列表，包含 name、args、id |
| tool_results | 工具结果列表 | 工具执行结果列表，包含 tool_call_id、result、status（success/error） |
| tool_retry_count | 整数 | 当前工具调用重试次数，超过上限则强制退出 |

**Agent 执行控制**

| 字段 | 类型 | 说明 |
|------|------|------|
| step_count | 整数 | 当前已执行步数，防止死循环（硬限制，如 max=10） |
| max_steps | 整数 | 最大允许步数 |
| is_complete | 布尔 | 标记 Agent 是否已完成任务，用于退出循环 |
| error_message | 字符串 | 错误信息，非空时表示出错，供错误处理节点读取 |

**输出**

| 字段 | 类型 | 说明 |
|------|------|------|
| final_answer | 字符串 | 最终合成的答案 |
| sources | 字符串列表 | 引用来源列表，用于 RAG 回答时附带出处 |
| confidence | 浮点数 | 最终回答的置信度，低于阈值时加"我不确定"前缀 |

**记忆与会话**

| 字段 | 类型 | 说明 |
|------|------|------|
| session_id | 字符串 | 会话标识，用于 Redis 中存取对话历史 |
| summary | 字符串 | 历史对话的压缩摘要（由摘要节点生成，节省 token） |

### 2.2 图节点定义

每个节点是一个处理函数，读取 state 中的某些字段，写入另一些字段：

| 节点 | 职责 | 读取 | 写入 |
|------|------|------|------|
| intent_classifier | 意图识别：分析用户输入 | messages | user_intent |
| context_compressor | 上下文压缩：历史过长时生成摘要 | messages | summary, messages(裁剪) |
| router | 路由：根据意图决定下一步 | user_intent | route_decision |
| direct_answer | 直接回答：无需工具/RAG，LLM 直接回复 | messages, summary | final_answer, is_complete |
| query_rewriter | 查询改写：优化检索词 | query | rewritten_query |
| rag_retriever | RAG 检索：向量 + BM25 混合召回 | rewritten_query, query | retrieved_docs, retrieval_confidence |
| retrieval_grader | 召回质量评估：判断是否召回了有用信息 | retrieved_docs, query | retrieval_confidence |
| answer_synthesizer | 答案合成：基于检索结果生成最终回答 | retrieved_docs, messages | final_answer, sources, confidence, is_complete |
| hallucination_checker | 幻觉检查：验证回答是否有依据 | final_answer, retrieved_docs | confidence, is_complete |
| tool_selector | 工具选择：LLM 决定调用哪些工具 | messages | tool_calls |
| tool_executor | 工具执行：实际调用工具，处理结果 | tool_calls | tool_results, tool_retry_count, error_message |
| tool_result_evaluator | 工具结果评估：判断结果是否满足需求 | tool_results | is_complete, tool_retry_count |
| error_handler | 错误处理：统一处理各类异常 | error_message, tool_retry_count | final_answer, is_complete |

### 2.3 图的边与条件路由

**无条件边**（直接跳转）：

- intent_classifier → context_compressor
- context_compressor → router
- query_rewriter → rag_retriever
- tool_selector → tool_executor
- error_handler → END

**条件边**（根据状态决定走向）：

| 起点节点 | 判断依据 | 可能走向 |
|----------|----------|----------|
| router | route_decision 字段 | direct_answer / rag_retrieval / tool_execution / handoff |
| retrieval_grader | retrieval_confidence > 0.6 | pass → answer_synthesizer / fail → direct_answer（兜底） |
| tool_executor | error_message / is_complete / tool_retry_count | complete → synthesizer / retry → tool_selector / error → error_handler |
| tool_result_evaluator | is_complete | done → answer_synthesizer / loop → tool_selector |
| answer_synthesizer | 是否有 RAG 结果 | 有 → hallucination_checker / 无 → END |
| hallucination_checker | confidence > 0.7 | pass → END / fail → 重新生成 |

### 2.4 完整状态流转图

```
                    ┌─────────────────┐
                    │ intent_classifier│  ← 识别用户意图
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │context_compressor│  ← 压缩历史上下文
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
              ┌─────│     router      │─────┐──────┐
              │     └─────────────────┘     │      │
              ▼                             ▼      ▼
    ┌─────────────────┐      ┌──────────────┐  ┌────────┐
    │ direct_answer   │      │query_rewriter│  │ handoff│→ END
    │ (LLM直答/兜底)  │      └──────┬───────┘  └────────┘
    └────────┬────────┘             │
             │              ┌───────▼────────┐
             │              │  rag_retriever  │  ← 向量+BM25检索
             │              └───────┬────────┘
             │              ┌───────▼────────┐
             │              │retrieval_grader │  ← 评估召回质量
             │              └───────┬────────┘
             │                ┌─────┴─────┐
             │            pass│           │fail
             │                ▼           ▼
             │   ┌──────────────────┐  (回到 direct_answer)
             │   │answer_synthesizer│  ← 合成最终答案
             │   └────────┬────────┘
             │            │
             │   ┌────────▼────────┐
             └──►│hallucination_chk│  ← 幻觉检查
                 └────────┬────────┘
                   ┌──────┴──────┐
               pass│             │fail
                   ▼             ▼
                  END       (重新生成)

    ──── 工具调用分支 ────

    ┌──────────────┐
    │ tool_selector │ ← LLM 选择工具
    └──────┬───────┘
    ┌──────▼───────┐
    │tool_executor  │ ← 实际执行工具
    └──────┬───────┘
           │
    ┌──────▼──────────────┐
    │tool_result_evaluator│ ← 评估结果
    └──────┬──────────────┘
       ┌───┴───┬──────┐
   complete  retry  error/give_up
       │       │      │
       ▼       ▼      ▼
  synthesizer  tool   error_handler → END
              _selector
```

---

## 3. 多 Agent 协作

### 3.1 Orchestrator 模式（推荐）

```
┌──────────────────┐
│  Orchestrator     │ ← 主 Agent，负责任务分解和分派
│  (规划 + 协调)    │
└───┬──────┬───────┘
    │      │
    ▼      ▼
┌───────┐ ┌───────┐ ┌───────┐
│研究Agent│ │代码Agent│ │审查Agent│ ← 子 Agent，各司其职
└───────┘ └───────┘ └───────┘
```

- Orchestrator 看全局，子 Agent 只关注自己的领域
- 子 Agent 之间**不直接通信**，都通过 Orchestrator 中转
- 适用：复杂任务需要多种能力组合

### 3.2 Pipeline 模式

```
Agent A → Agent B → Agent C → 输出
(收集信息)  (分析)    (生成报告)
```

- 前一个输出是后一个输入，线性流程
- 适用：步骤固定的流水线任务

### 3.3 Debate 模式

```
    Agent A ──观点──► Agent B
       ▲                 │
       └──反馈/反驳──────┘
           (多轮后达成共识)
```

- 多个 Agent 互相审查，提高答案质量
- 适用：需要高可靠性的场景（如代码审查、方案评审）

### 3.4 关键设计点

- **上下文传递**：子 Agent 之间通过 Orchestrator 传递必要信息，不是全量上下文
- **最终决策权**：Orchestrator 有最终裁决权，避免死锁
- **超时控制**：每个子 Agent 有独立超时，总流程也有超时

---

## 4. Function Calling / Tool 工具调用

### 4.1 工具定义

每个工具需要声明：名称、描述、参数（类型、是否必填）、返回格式。

### 4.2 工具描述的重要性

工具描述写得好不好，直接决定 LLM 能不能选对工具：

| 差的描述 | 好的描述 |
|----------|----------|
| "search" | "在互联网上搜索实时信息，适用于需要最新数据的场景" |
| "query db" | "执行只读 SQL 查询，仅限 SELECT 语句，禁止写操作" |

### 4.3 工具调用失败处理

| 失败类型 | 处理方式 |
|----------|----------|
| 网络超时 | 指数退避重试（1s, 2s, 4s），最多 3 次 |
| 参数错误 | 把错误信息返回 LLM，让它修正参数重新调用 |
| 权限不足 | 告知用户，请求授权 |
| 工具不存在 | 告知 LLM 可用工具列表，重新选择 |
| 未知错误 | 记录日志，返回通用错误信息给用户 |

关键原则：
- **不要静默吞错误**：LLM 需要知道失败了才能调整策略
- **重试有上限**：防止无限循环
- **错误信息结构化**：包含 status、message、retryable 字段

---

## 5. 上下文管理与 Token 成本

### 5.1 上下文窗口的构成

```
┌──────────────────────────────────────┐
│  系统提示词 (固定，~500 tokens)         │
├──────────────────────────────────────┤
│  对话历史摘要 (压缩后，~1k tokens)      │
├──────────────────────────────────────┤
│  RAG 检索结果 (动态，~2k tokens)        │
├──────────────────────────────────────┤
│  最近 N 轮对话 (原文，~3k tokens)       │
├──────────────────────────────────────┤
│  当前用户输入 (~200 tokens)             │
└──────────────────────────────────────┘
```

### 5.2 压缩策略

| 策略 | 方法 | 适用场景 |
|------|------|----------|
| 滑动窗口 | 只保留最近 N 轮 | 简单场景，历史不太重要 |
| 摘要压缩 | 用小模型把历史压缩成摘要 | 需要保留全局信息 |
| 重要性裁剪 | 保留工具调用结果，删除闲聊 | 工具密集型 Agent |
| 分层缓存 | 系统提示 + 摘要 + 最近对话 | 通用推荐方案 |

### 5.3 上下文过长的分级处理

| token 用量 | 处理方式 |
|------------|----------|
| < 50% 上限 | 正常 |
| 50%~80% | 启用摘要压缩，压缩早期对话 |
| 80%~95% | 激进压缩，只保留系统提示 + 最近 2 轮 + 摘要 |
| > 95% | 告知用户上下文已满，建议开新会话 |

### 5.4 Token 成本控制

- **输入/输出分别计价**：输出通常比输入贵 3-5 倍
- **长上下文成本高**：100K tokens 的单次调用 > 10 次 10K tokens 的调用之和
- **缓存命中**：相同的系统提示前缀可以缓存（Anthropic prompt caching）
- **模型分级**：简单任务用小模型，复杂任务用大模型

---

## 6. 记忆系统：短期记忆与长期记忆

### 6.1 对比

| | 短期记忆 | 长期记忆 |
|---|---|---|
| 存储 | Redis（内存） | 向量数据库（持久化） |
| 生命周期 | 当前会话（TTL 24h） | 跨会话持久化 |
| 内容 | 对话历史、中间状态、工具调用结果 | 用户偏好、关键事实、历史决策 |
| 检索方式 | 按 session_id 精确查找 | 按语义相似度检索 |
| 读写频率 | 每轮都读写 | 关键节点才写入 |

### 6.2 记忆的读写时机

| 时机 | 操作 |
|------|------|
| 会话开始 | 从长期记忆中检索与当前问题相关的历史 |
| 每轮对话 | 更新短期记忆（追加消息） |
| 关键事实出现 | 写入长期记忆（如用户说"我是后端工程师"） |
| 会话结束 | 将对话摘要写入长期记忆 |

### 6.3 长期记忆的结构

长期记忆存储在向量数据库中，按类型分区：

- **用户画像**：角色、偏好、技术栈
- **关键事实**：项目信息、架构决策、约定
- **过往交互**：历史对话摘要、上次讨论的结论
- **反馈记录**：用户纠正过的行为、确认过的做法

---

## 7. RAG 系统：文本分块、Embedding、向量检索

### 7.1 文本分块策略

| 策略 | 方法 | 优点 | 缺点 |
|------|------|------|------|
| 固定大小 | 按 token 数切，如 512 tokens | 简单可控 | 可能切断语义 |
| 语义分块 | 按段落/标题/句子边界切 | 保留语义完整性 | 块大小不均匀 |
| 递归分块 | 先大块再小块 | 平衡语义和大小 | 实现稍复杂 |

**关键参数**：
- chunk size：一般 256~1024 tokens
- chunk overlap：20%~50%，避免边界丢失信息

### 7.2 Embedding 流程

```
原始文档
    │
    ▼
文本分块 (Chunking)
    │
    ▼
┌──────────────────┐
│ Embedding 模型     │  ← 文本 → 高维向量
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ 向量数据库         │  ← 存储 + 建立索引
└──────────────────┘
```

### 7.3 检索流程

```
用户问题
    │
    ├─ 向量检索: 问题 → embedding → 相似度搜索 → Top-K
    │
    ├─ 关键词检索: BM25 → Top-K
    │
    ▼
合并去重 → 重排序 → 返回 Top-N
```

### 7.4 RAG 召回失败的应对

| 召回置信度 | 处理方式 |
|------------|----------|
| > 0.6 | 正常回答，附带来源引用 |
| 0.3 ~ 0.6 | 用 LLM 自身知识补充，标注"参考性质" |
| < 0.3 | 告知用户"未找到相关信息" |

**提升召回率的手段**：

| 手段 | 说明 |
|------|------|
| 查询改写 | 用 LLM 把用户口语化问题改写成检索友好的形式 |
| 混合检索 | 向量检索 + BM25 关键词检索并行 |
| 多路召回 | 拆分子问题分别检索，合并去重 |
| chunk 优化 | 调整分块大小、重叠比例 |
| embedding 升级 | 换更好的 embedding 模型 |

---

## 8. Redis 在 Agent 中的角色

| 用途 | 说明 | TTL |
|------|------|-----|
| 对话历史缓存 | 按 session_id 存取消息列表 | 24h |
| 工具结果缓存 | 相同输入的工具调用直接返回缓存，减少重复计算 | 1h |
| 会话元数据 | 用户信息、偏好、当前任务状态 | 24h |
| 限流/防刷 | 令牌桶控制 API 调用频率 | 1min |
| 分布式锁 | 多 Agent 并发时协调资源访问 | 30s |
| 向量存储 | Redis Stack 支持向量检索，小规模场景够用 | 持久 |

---

## 9. 提示词设计

### 9.1 系统提示词模板结构

```
角色定义 → 能力边界 → 工具使用规则 → 回答规范 → 输出格式
```

各部分要点：

| 部分 | 要点 |
|------|------|
| 角色定义 | 明确"你是谁"，聚焦任务领域 |
| 能力边界 | 列出能做和不能做的事，减少无关输出 |
| 工具规则 | 什么时候用工具、用哪个、最多调几次 |
| 回答规范 | 基于文档回答、附带来源、不确定时说"不知道" |
| 输出格式 | 指定 JSON/Markdown 等格式，方便下游解析 |

### 9.2 提示词设计原则

| 原则 | 说明 |
|------|------|
| 角色明确 | 减少无关输出，聚焦任务 |
| 边界清晰 | 告诉 LLM 什么**不能做**，比能做什么更重要 |
| 输出格式固定 | 方便下游解析，减少格式错误 |
| few-shot 示例 | 一个好示例胜过十句描述 |
| 负面示例 | 告诉 LLM "不要这样做"，比正面描述更有效 |
| 分层组织 | 用标题、列表、分隔符组织，LLM 更容易遵循 |

---

## 10. 可靠性与异常处理

### 10.1 Agent 死循环

**常见原因**：
- 工具返回结果后 LLM 不满足，反复调用同一工具
- 条件判断永远不退出循环节点
- 多 Agent 互相触发

**解决方案**：

| 手段 | 说明 |
|------|------|
| 硬限制 | step_count >= max_steps 时强制退出 |
| 重复检测 | 连续 2 次相同工具 + 相同参数 → 中断 |
| 超时机制 | 单步超时 30s，总流程超时 5min |
| LLM 自省 | 每步让 LLM 判断"我是否已经得到答案了" |

### 10.2 幻觉问题

| 手段 | 说明 |
|------|------|
| RAG grounding | 强制 LLM 只基于检索到的内容回答，附带来源引用 |
| 置信度评估 | 让 LLM 输出置信度，低于阈值时说"我不确定" |
| 事实核查 | 专门的子 Agent 验证回答与源文档是否一致 |
| 提示词约束 | "如果文档中没有相关信息，明确告知用户" |

### 10.3 工具调用失败

| 失败类型 | 处理方式 |
|----------|----------|
| 网络超时 | 指数退避重试（1s, 2s, 4s），最多 3 次 |
| 参数错误 | 把错误信息返回 LLM，修正参数重试 |
| 权限不足 | 告知用户，请求授权 |
| 工具不存在 | 返回可用工具列表，让 LLM 重新选择 |
| 未知错误 | 记录日志，返回通用错误信息给用户 |

关键原则：
- **不要静默吞错误** — LLM 需要知道失败了才能调整策略
- **重试有上限** — 防止无限循环
- **错误信息要具体** — "SQL 语法错误" 比 "执行失败" 有用得多

### 10.4 上下文过长

| token 用量 | 处理方式 |
|------------|----------|
| < 50% 上限 | 正常 |
| 50%~80% | 启用摘要压缩，压缩早期对话 |
| 80%~95% | 激进压缩，只保留系统提示 + 最近 2 轮 + 摘要 |
| > 95% | 告知用户上下文已满，建议开新会话 |

---

## 11. 技术选型建议

| 组件 | 推荐 | 备选 |
|------|------|------|
| 编排框架 | LangGraph | LangChain, CrewAI |
| LLM | Claude API (200K context) | GPT-4o, DeepSeek |
| 向量数据库 | Milvus / Qdrant | Chroma, FAISS, Redis Stack |
| 缓存/会话 | Redis | 本地文件（开发阶段） |
| Embedding | text-embedding-3-small | bge-large, Cohere |
| 监控追踪 | LangSmith | LangFuse, 自建日志 |
| 开发框架 | FastAPI + Python | Node.js |

---

> 本文档为架构设计参考，具体实现请结合业务场景调整。
